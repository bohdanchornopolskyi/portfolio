services:
  mongodb:
    image: mongo:latest
    container_name: bohdan-portfolio-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: bohdan-portfolio

volumes:
  mongo_data:
